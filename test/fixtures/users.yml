<% require 'bcrypt' %>
one:
  email: one@example.com
  encrypted_password: <%= BCrypt::Password.create('password', cost: 1) %>

two:
  email: two@example.com
  encrypted_password: <%= BCrypt::Password.create('password', cost: 1) %>

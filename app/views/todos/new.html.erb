<% content_for :title, "Todo を作成" %>

<div class="row justify-content-center">
  <div class="col-md-8">
    <div class="card shadow-sm">
      <div class="card-header">
        <h4 class="mb-0">新しい Todo</h4>
      </div>
      <div class="card-body">
        <%= render "form", todo: @todo %>
      </div>
    </div>
  </div>
</div>
